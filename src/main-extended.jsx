import React from "react";
import ReactDOM from "react-dom/client";
import Extended from "./components/serieFilms";

ReactDOM.createRoot(
  document.getElementById("root")
).render(
  <React.StrictMode>
    <Extended />
  </React.StrictMode>
);
